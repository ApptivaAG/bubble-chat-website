<section class="partials-customers">
    {% for item in customers.items %}
        <div class="customer">
            {% image item.logo,
            item.name,
            {"class":"logo", "sizes":"100vw", "widths":[200] }
            %}
        </div>
    {% endfor %}

</section>
