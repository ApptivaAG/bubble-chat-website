<section class="partials-customers">
    {% for item in customers.items %}
        <div class="customer">
            <img name="{{ item.name }}" src="{{ item.logo }}">
        </div>
    {% endfor %}

</section>
